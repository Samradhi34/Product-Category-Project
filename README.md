# Product-Category-Project
A basic learning project using Spring Boot and PostgreSQL. Supports CRUD operations for products and categories, along with image upload functionality. Handles multipart image files, stores them on the server, and saves file paths in the database to link images with products.
